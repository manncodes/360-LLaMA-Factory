### Benchmark config for Baseline (no scaling)
model_name_or_path: meta-llama/Meta-Llama-3-8B-Instruct
template: llama3

### Long context settings (no rope scaling)
rope_scaling: none
model_max_length: 8192

### Optimization
flash_attn: auto
use_cache: true

### Inference settings
do_sample: false
temperature: 0.1
max_new_tokens: 100